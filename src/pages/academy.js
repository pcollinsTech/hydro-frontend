import React from "react";
import Layout from "../layout/Layout";

const Academy = props => {
    return (
        <Layout props={props}>
            <h1>Academy</h1>
        </Layout>
    );
};

export default Academy;
