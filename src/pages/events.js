import React from "react";
import Layout from "../layout/Layout";

const Events = (props) => {
  return (
    <Layout props={props}>
      <div className="container">
        <div className="row">
          <h1>Events</h1>
        </div>
      </div>
    </Layout>
  );
};

export default Events;
