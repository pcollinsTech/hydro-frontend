import React from "react";
import Layout from "../layout/Layout";
const Groups = (props) => {
  return (
    <Layout props={props}>
      <h1>Groups</h1>
    </Layout>
  );
};

export default Groups;
